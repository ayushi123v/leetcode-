class Solution {
    public int maxDepth(TreeNode root) {
        if(root == null){
            return 0 ; 
        }
        int leftside = maxDepth(root.left);
        int rightside = maxDepth(root.right);
        int height = Math.max(leftside,rightside)+1;
        return height ;

    }
}
